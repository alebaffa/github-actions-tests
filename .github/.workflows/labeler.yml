name: "Labeler"
on:
  - pull_request_target

jobs:
  pr-labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v4.1.0
      with:
        sync-labels: true
        repo-token: "${{ secrets.GITHUB_TOKEN }}"